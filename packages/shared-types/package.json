{
  "name": "@pva/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "Zod schemas + types shared across web, api-gateway, orchestrator and SDK",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts",
    "./enums": "./src/enums.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: lint'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
