{
  "name": "@pva/sdk-ts",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript SDK consuming the api-gateway REST + WebSocket contracts",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: lint'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@pva/shared-types": "workspace:*",
    "socket.io-client": "^4.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
